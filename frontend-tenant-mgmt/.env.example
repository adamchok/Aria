VITE_API_BASE_URL=
# When running via Vite's dev proxy you can leave VITE_API_BASE_URL empty
# and requests to /api will be forwarded to VITE_API_PROXY_TARGET below.
VITE_API_PROXY_TARGET=http://localhost:8000
